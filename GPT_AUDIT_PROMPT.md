# 可直接发送给 GPT 的审核指令

请完整阅读压缩包中的 `GPT_REVIEW_HANDOFF.md`，再阅读福建官方 PDF 和全部源码。

注意交接材料第 9–12 节保留的是历史快照；本次应以第 13 节和 `fj-2026-45-preview-v5` 实际源码为准。重点复查 12 个临床入口 + 跨治疗辅助索引是否仍有错归，目录只读门禁是否有效，以及根管冲洗数量未获院方确认时的安全提示。不要把 114 项目录显式覆盖误判为 114 项临床流程验收完成。

第一步不要马上挑代码问题，请先复述你对开发需求的理解，重点说明：

1. 为什么这不是 711/900/316/400 四个固定套餐按钮；
2. “治疗项目、治疗路径、本次实际操作、候选收费项目”分别是什么；为何常规根管不能强制按第 1/2 期分开；
3. 医生选择实际实施项与系统自动规则的职责边界；
4. 互斥、依附、最高限价、牙位/部位、套餐复用分别怎样工作；
5. 为什么广州竞品和医院现有套餐只能参考，福建官方文件才是收费规则依据。

确认理解后，再做联合审核：

- 逐项核对 `catalog.js` 与 PDF；
- 审核 `treatment-rules.js` 的根管流程是否真实、完整、不过度推荐项目；
- 特别核查逐项实际根管数量、异常加收数量、同编码乳牙拔除 11 元条件价及不并收限制；
- 审核 `app.js` 的主项、加收、减收、扩展、自主定价、数量、牙位互斥和套餐安全；
- 审核 `index.html` 是否清楚标识内部体验版和 2026-10-15 执行日期；
- 用 P0/P1/P2 输出问题，每项提供文件位置、复现方式、风险和修改建议；
- 文件没有明确写明、需要临床经验判断的地方，请标注“需要福建医院临床专家确认”，不要自行编造规则。

最后给出下一阶段开发优先级，并明确哪些条件满足前仍不能用于正式收费。
